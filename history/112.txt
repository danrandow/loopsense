iteration-0.yaml
Give entity2 and entityR2 a real label and short notes pointing to their full entity files (Delivery drafted entity2-v0.md and entityR2-v0.md). Dan-approved 2026-09-21 ("entity2 needs a label").
WHY: entity2 still said the practitioner-readable description was missing, and entityR2 said Not Yet; both were stale once D1 and D2 were drafted. Rule 5: label = headline, notes = short summary ending Full entity path.
BEFORE:
  - id: entity2
    label: First Artifact — The Map
    notes: 'The artifact exists. It is the map itself: `base.yaml` (topology), four `SKILL.md` files

      (agent roles), `knowledge/` (agent knowledge base, Obsidian-style), and scenario YAMLs

      (iteration snapshots).


      **What is missing:** a practitioner-readable description. Someone who has never seen this

      project cannot yet pick it up and implement it. That is the current delivery task.

      '
  - id: entityR2
    label: Delivery Reality — Not Yet
AFTER:
  - id: entity2
    label: Start-here page and loop spec, draft
    notes: 'First draft, untested: a practitioner "start here" page (D1) and the @loopsense content-loop
      spec with signal ledger (D2).

      Underneath it sits the map itself: `base.yaml`, the four `SKILL.md` files and the knowledge files.

      Not yet read by anyone outside the team. D3 (verdict rule) not started.

      Full entity: agents/delivery/generates/entity2-v0.md

      '
  - id: entityR2
    label: D1 and D2 drafted; D3 waits on PM
    notes: 'D1 and D2 drafted in one session. The map already supports the content loop with no new elements
      (return edge is entityR4B plus a file convention). D3 needs the definition of improved from the PM.
      No escalation to Exec.

      Full entity: agents/delivery/generates/entityR2-v0.md

      '
