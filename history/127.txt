FILE: knowledge/standing-rules.md
WHY: Dan-approved, from iteration-0 retro synthesis (agents/loopy/returns/retro-synthesis-iteration0.md). Three fixes: (1) de-duplicate rule 7, which appeared twice near-identically (PM flagged this — had to guess which was authoritative); (2) make `id` mandatory on every log entry, YAML and non-YAML, removing the "some entries have no id; ignore those" caveat (Exec and Delivery both wanted this — manual id-scan was a recurring friction point); (3) name the read-recompute-retry step in rule 6.3 as the expected path under concurrent writers, not a failure mode (GTM hit a live id collision with Exec mid-session; one retry fixed it).

BEFORE (line 18):
   1. **Pick the id:** the highest integer `id` in the log plus 1 (some entries have no `id`; ignore those).

AFTER (line 18):
   1. **Pick the id:** every log entry has an `id`, assigned in order. Use the highest `id` in the log plus 1.

BEFORE (line 20, last sentence of the paragraph):
      ```
      {
        "id": 98,
        ...
      }
      ```
      Keep `id` and `status` on adjacent lines in that order. `action` uses the existing words (create, update, fix, extend, rename, delete). The note says who approved it and gives the exact Was/Now for every value you change, including any note you overwrite.

AFTER: unchanged except the sentence "If the Edit fails, someone else appended: re-read the log, recompute the id, and retry. Do not fall back to Write." (inside step 3's lead-in) gains one clause:
      "If the Edit fails, someone else appended: re-read the log, recompute the id, and retry — this is the normal path when two agents close out entries the same day, not a sign anything went wrong. Do not fall back to Write."

BEFORE (line 44):
   **Other files.** For any non-YAML write, append a log entry the same way (Edit at the end, never Write) before writing; `status` and `record` are optional.

AFTER (line 44):
   **Other files.** For any non-YAML write, append a log entry the same way (Edit at the end, never Write) before writing; every entry still gets an `id` per rule 6.1 — `status` and `record` are optional.

BEFORE (lines 46-47, rule 7 duplicated verbatim, second block adding the PM-routing sentence):
7. **Record every harness change.** ... In retros, ask for the agent's own experience first; do not lead with our reasons.
7. **Record every harness change.** ... The records are addressed to PM: Dan and Loopy bring feedback, rationale and observations to PM, and PM decides what reaches the team and when. The log and history are an audit trail, not instructions; agents may read them but they never override PM's direction. Delivery documents the design from what PM passes on, not directly from the raw log. Dan and Loopy do not direct other agents past PM. In retros, ask for the agent's own experience first; do not lead with our reasons.

AFTER: first (shorter) block deleted; second (fuller, PM-routing) block kept as the sole rule 7.
