WHAT: Retire the per-file audit process — append-only entries in loopsense.log.json plus history/{id}.txt change records — and freeze the legacy ledger after entry 197. Adopt the Git-native audit policy (knowledge/audit-policy.md): Git commits and diffs record exactly what changed; short decision records in decisions/ (Context, Decision, Why, Consequences) explain material owner/PM decisions; iteration-N.yaml records current iteration state; runtime task/session logs record operational failures, retries and aborted runs. Publication approvals and post URLs move to the dedicated GTM publication ledger (agents/gtm/knowledge/publication-ledger.md), owned by GTM and referenced by knowledge/social-agent-publishing-standard.md. loopsense.log.json and all existing history/ files are preserved unchanged as an immutable historical audit through ID 196; entry 197 and this record are the final legacy entries. Historical duplicate ids 67-69 remain untouched (documented in entry 196 and history/196.txt).
WHY: The per-file ledger duplicated Git provenance: commits and diffs already record exact before/after state and rollback history, while the log-first procedure taxed every write with an entry and (for harness/YAML changes) a BEFORE/AFTER record. Owner-approved harness change (Dan, 2026-09-24). Material decision record: decisions/2026-09-24-git-native-audit.md. The migration file changes accompanying this decision are the first batch governed by the new policy: their provenance is the Git commit, not the ledger. No publishing gate or authorisation changed.
