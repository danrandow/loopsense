agents/pm/SKILL.md — enumerate the retro artefact in the write boundary

WHY: All four role retros for iteration 1 reported that `retro-iterationN.md` is not enumerated in any write boundary; each wrote under owner instruction and iteration-0 precedent. Owner approved recommendation 4 (2026-09-23): enumerate it in every agent's write boundary. Rule 7 record for a skill change.

BEFORE:
- `agents/pm/generates/entity0-v{n}.md` — the aligned bet (full); one file per iteration
- `agents/pm/knowledge/pm.md` — running knowledge: pointer to current bet, practitioner signals, sources, iteration log

AFTER:
- `agents/pm/generates/entity0-v{n}.md` — the aligned bet (full); one file per iteration
- `agents/pm/generates/retro-iterationN.md` — your own harness retro, written when the owner triggers the retro
- `agents/pm/knowledge/pm.md` — running knowledge: pointer to current bet, practitioner signals, sources, iteration log
