agents/delivery/SKILL.md — resolve the conflicting Exec-source paths to the canonical handoff source

WHY: Delivery's iteration-1 retro reported shipping work around a path conflict: its "Before you act" list named `agents/exec/knowledge/exec-v0.md` while the session protocol named `agents/exec/generates/entity1-v{n}.md`, so Delivery read both. Owner approved recommendation 7 (2026-09-23): resolve to the actual canonical handoff source. The Exec brief is `agents/exec/generates/entity1-v{n}.md` (Exec's own skill and standing rule 4 put the brief in generates/; exec-v0.md holds the thesis and certification history). Also corrected in the same list: item 5 labelled the aligned bet as `entity1` — entity1 is Exec's brief, the aligned bet is entity0 — so it now cites `agents/pm/generates/entity0-v{n}.md` and entity1 is named once. Rule 7 record for a skill change.

BEFORE (region 1):
2. **The Exec brief** (`agents/exec/knowledge/exec-v0.md`): who is the customer, what is the positioning,
   what are the constraints. You are building for the customer the Exec certified. If the brief
   is missing or vague, return the iteration to Exec — you cannot build for a vague customer.

BEFORE (region 2):
5. **entity1** — the current aligned bet from PM. The artifact must serve the bet. If it doesn't,
   flag it. That is a delivery failure that starts upstream.

AFTER (region 1):
2. **The Exec brief** (`agents/exec/generates/entity1-v{n}.md`): who is the customer, what is the positioning,
   what are the constraints. You are building for the customer the Exec certified. If the brief
   is missing or vague, return the iteration to Exec — you cannot build for a vague customer.

AFTER (region 2):
5. **entity0** (`agents/pm/generates/entity0-v{n}.md`) — the current aligned bet from PM. The artifact must serve the bet. If it doesn't,
   flag it. That is a delivery failure that starts upstream.
